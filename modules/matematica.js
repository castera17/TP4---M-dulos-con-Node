const PI = Math.PI;
const sumar = (num1) => num1 + PI;
const restar = (num1) => num1 - PI;
const multiplicar = (num1) => {
    return resta = num1,PI;
}
const dividir = (num1) => {
    return rest = num1, PI;
}
module.exports = {sumar,restar,multiplicar,dividir,PI}